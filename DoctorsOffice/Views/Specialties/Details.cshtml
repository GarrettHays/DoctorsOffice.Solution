@{
  Layout = "_Layout";
}

@model DoctorsOffice.Models.Specialty;

<h2>Specialty Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Description): @Html.DisplayFor(model => model.Description)</h3>

@if(@Model.JoinEntities.Count == 0)
{
  <p>This Specialty does not contain any Doctors</p>
}
else
{
  <h4>Doctors the Specialty contains:</h4>
  <ul>
  @foreach(var join in Model.JoinEntities)
  {
    <li>@join.Doctor.Name</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to Specialties", "Index",null, null, new { @class = "btn btn-primary btn-primary:hover"})
@Html.ActionLink("Edit Specialty", "Edit", null, new { id = Model.SpecialtyId }, new { @class = "btn btn-primary btn-primary:hover"})
@Html.ActionLink("Delete Specialty", "Delete", null, new { id = Model.SpecialtyId }, new { @class = "btn btn-primary btn-primary:hover"})</p>